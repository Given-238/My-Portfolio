# GUI Quiz Game (Tkinter) — Quiz Game with Timer, Sounds & Scoreboard

A user-friendly quiz game built in Python using Tkinter, with:
- Questions loaded from `questions.txt`
- Difficulty levels: EASY / MEDIUM / HARD
- 10 second timer per question with progress bar
- Animations (background flash) and sound effects (correct/wrong)
- Persistent scoreboard saved to `scoreboard.json`

## Requirements
- Python 3.8+
- pip install -r requirements.txt

## Setup
1. Clone or download repository.
2. `cd quiz-game-gui`
3. Install dependency: